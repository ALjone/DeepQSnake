To do:

Rewrite checkpoints to just save a the model of a training session along with its hyperparameter file (pickle?)
Comment and clean up code
Make multi-agent setup
Try same setup with different games
Input to linear layers that says something about whether or not that state has already been visited

Saves the plots 